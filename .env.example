# VerityAPI base URL. Supabase configuration lives in VerityAPI only.
VITE_API_BASE_URL="http://localhost:8080/api/v1"

# Use "demo" to keep local demo data and bypass API auth during demos.
# Use "api" for VerityUI -> VerityAPI -> Supabase flow.
VITE_RUN_MODE=demo
